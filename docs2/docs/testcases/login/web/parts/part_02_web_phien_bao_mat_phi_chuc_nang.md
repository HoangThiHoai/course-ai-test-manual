# Test Cases — `LOGIN` · Web · Part 02 — Phiên đăng nhập, Quên mật khẩu, bảo mật & phi chức năng

> ⬆️ Index: [../../TEST_CASES_LOGIN_SUMMARY.md](../../TEST_CASES_LOGIN_SUMMARY.md) · 📄 Requirements: [../../../../requirements/login/web/requirements_login_web.md](../../../../requirements/login/web/requirements_login_web.md) · ⬅️ Part trước: [part_01_web_giao_dien_nhap_lieu.md](part_01_web_giao_dien_nhap_lieu.md)

| Mục | Giá trị |
|---|---|
| **TC trong part** | `CRM_LOGIN_TC_023` → `CRM_LOGIN_TC_060` — **38 TC · 61 biến thể/mục kiểm** |
| **Vòng** | V2b Luồng, phiên & Quên mật khẩu (`023`–`041`) · V3 Technical (`042`–`048`) · V4 Non-functional (`049`–`053`) · Bổ sung sau review 23-09-2026 (`054`–`060`, chia theo vòng ở mục cuối) |
| **Độ hạt** | GỘP — như part 01 |
| **Tài khoản** | 🔒 Admin: `admin@example.com` + `PASSWORD_ADMIN` · Project Manager: `EMAIL_PM` + `PASSWORD_PM` · Customer: `EMAIL_CUSTOMER` + `PASSWORD_CUSTOMER` — tất cả trong `.env`, **KHÔNG** ghi giá trị thật vào tài liệu |
| **Môi trường dùng chung** | TC nào tạo dữ liệu (`CRM_LOGIN_TC_031`) phải dọn đúng dữ liệu mình tạo ở bước cuối |

## V2b — Luồng nghiệp vụ & trạng thái phiên

### Quy tắc nghiệp vụ khi đăng nhập

| TC ID | REQ ID | Module | Risk Level | Test Scenario | Pre-Condition | Test Steps | Test Data | Expected Result | Priority | Automation | Auto Type | Tags |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| CRM_LOGIN_TC_023 | REQ-LOGIN-41, REQ-LOGIN-05 | Đăng nhập | High | Sai mật khẩu nhiều lần liên tiếp không khoá tài khoản, sau đó đăng nhập đúng vẫn được | Chrome, cửa sổ ẩn danh — chưa đăng nhập | 1. Mở `https://crm.anhtester.com/admin/authentication`<br>2. Nhập Email `admin@example.com`, Password `SaiMatKhau_TC023`, bấm `Login`<br>3. Lặp lại bước 2 thêm 5 lần (tổng 6 lần sai)<br>4. Nhập Email `admin@example.com`, Password là giá trị `PASSWORD_ADMIN` trong `.env`, bấm `Login` | Password sai: `SaiMatKhau_TC023` (6 lần) | 2. Đúng 1 dải báo lỗi `Invalid email or password`<br>3. Cả 5 lần đều **chỉ** có dải `Invalid email or password` — không có thông báo khoá, tạm ngưng, chờ vài phút hay CAPTCHA<br>4. Đăng nhập thành công, vào `https://crm.anhtester.com/admin/` | High | Yes | UI | @Regression @Security |
| CRM_LOGIN_TC_024 | REQ-LOGIN-16 | Đăng nhập | Medium | 🐞 Sau khi đăng nhập thất bại, ô Email phải giữ lại email vừa nhập | Chrome, **cửa sổ ẩn danh mới** — trình duyệt chưa lưu email nào, nên nó không thể tự điền lại thay máy chủ | 1. Mở `https://crm.anhtester.com/admin/authentication`<br>2. Nhập Email `qa_tc024_20260921@auto.test`<br>3. Nhập Password `SaiMatKhau_TC024`<br>4. Bấm `Login`<br>5. Nhìn ô Email sau khi trang nạp lại | Email: `qa_tc024_20260921@auto.test`<br>Password: `SaiMatKhau_TC024` | 4. Trang nạp lại, dải báo lỗi `Invalid email or password`<br>5. Ô Email **đang hiện sẵn** `qa_tc024_20260921@auto.test` — người dùng chỉ phải gõ lại mật khẩu<br><br>🐞 **TC thiết kế để FAIL** trên bản hiện tại — PO xác nhận là lỗi (`AMB-LOGIN-10`), bug đang mở `BUG_login_1787226514_TC016`. FAIL ở bước 5 là kết quả đúng — **không** sửa TC<br><br>🔧 Ghi chú kỹ thuật (cần DevTools): xem mã nguồn trang (View Source) sau bước 4 — ô Email phải mang giá trị email vừa gửi. Chấm ở mã nguồn vì màn hình có thể bị trình duyệt tự điền đánh lừa | Medium | Yes | UI | @Regression @KnownBug @TechCheck |

### Bảo vệ URL & điều hướng theo trạng thái phiên

> **Bảng chuyển trạng thái phiên** (ma trận trạng thái — index requirements mục 7) — mỗi ô là một TC:
>
> | Từ trạng thái | Hành động | Đến trạng thái | TC |
> |---|---|---|---|
> | Chưa đăng nhập | Gửi thông tin đúng | Đã đăng nhập | `006`, `016`, `017` |
> | Chưa đăng nhập | Gửi thông tin sai/thiếu | Chưa đăng nhập + báo lỗi | `009`–`015`, `018` |
> | Chưa đăng nhập | Mở URL nội bộ | ❌ Bị chặn → trang đăng nhập | `025`, `026` |
> | Đã đăng nhập | Mở trang đăng nhập / Quên mật khẩu | ❌ Bị chặn → Dashboard | `027`, `028` |
> | Đã đăng nhập | Logout, không có timer | Đã đăng xuất | `008` (Admin), `056` (Project Manager) |
> | Đã đăng nhập, mở 2 tab | Logout ở tab B, thao tác tiếp ở tab A | ❌ Tab A bị chặn → trang đăng nhập | `057` |
> | Đã đăng nhập | Logout, có timer | Đã đăng nhập + hộp xác nhận | `031` |
> | Hộp xác nhận | Bấm `Logout` trong hộp | Đã đăng xuất | `031` |
> | Đã đăng xuất | Mở URL nội bộ / bấm Back | ❌ Bị chặn → trang đăng nhập | `029` |
> | Đã đăng xuất, còn cookie ghi nhớ | Mở `/admin/` | ❌ Không tự đăng nhập lại | `030` |
> | Đã đăng nhập, để yên > 1 giờ | Mở URL nội bộ | ❌ Hết phiên → trang đăng nhập | `032` |
> | Đã đăng nhập, thao tác liên tục > 1 giờ | Mở URL nội bộ | Vẫn đăng nhập | `033` |

| TC ID | REQ ID | Module | Risk Level | Test Scenario | Pre-Condition | Test Steps | Test Data | Expected Result | Priority | Automation | Auto Type | Tags |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| CRM_LOGIN_TC_025 | REQ-LOGIN-17 | Bảo vệ phiên | High | Chưa đăng nhập mà mở thẳng URL trong khu quản trị thì bị đưa về trang đăng nhập | Chrome, cửa sổ ẩn danh — chưa đăng nhập | 1. Dán URL theo biến thể vào thanh địa chỉ, nhấn Enter<br>2. Quan sát thanh địa chỉ và nội dung trang | **Mọi biến thể cho cùng kết quả:**<br>`a` `https://crm.anhtester.com/admin/clients`<br>`b` `https://crm.anhtester.com/admin/projects`<br>`c` `https://crm.anhtester.com/admin/` | 1. Trình duyệt tự chuyển trang<br>2. Dừng ở `https://crm.anhtester.com/admin/authentication`, hiện biểu mẫu đăng nhập; **không** thoáng thấy dữ liệu khách hàng, dự án hay Dashboard nào<br><br>🔧 Ghi chú kỹ thuật (cần DevTools): chỉ chấm **điểm dừng**. Mã chuyển hướng ghi nhận được là 307 (`AMB-LOGIN-17`) — **không** dùng làm căn cứ chấm | Critical | Yes | UI | @Smoke @Security @CriticalPath @TechCheck |
| CRM_LOGIN_TC_026 | REQ-LOGIN-18 | Bảo vệ phiên | Medium | Bị đưa về trang đăng nhập thì hệ thống không nhớ URL đích ban đầu | Chrome, cửa sổ ẩn danh — chưa đăng nhập | 1. Dán `https://crm.anhtester.com/admin/clients` vào thanh địa chỉ, nhấn Enter<br>2. Đọc toàn bộ địa chỉ trên thanh địa chỉ<br>3. Nhập Email `admin@example.com`, Password là giá trị `PASSWORD_ADMIN` trong `.env`, bấm `Login`<br>4. Quan sát trang sau đăng nhập | URL đích ban đầu: `https://crm.anhtester.com/admin/clients` | 1. Chuyển về trang đăng nhập<br>2. Địa chỉ đúng `https://crm.anhtester.com/admin/authentication` — **không** có phần đuôi `?redirect=`, `?return_url=` hay dấu `?` nào<br>4. Vào Dashboard `https://crm.anhtester.com/admin/` — **không** quay lại trang Customers | Medium | Yes | UI | @Regression |
| CRM_LOGIN_TC_027 | REQ-LOGIN-19 | Bảo vệ phiên | Medium | Đang đăng nhập mà mở trang đăng nhập thì được đưa về Dashboard | Đã đăng nhập bằng tài khoản theo biến thể | 1. Dán `https://crm.anhtester.com/admin/authentication` vào thanh địa chỉ, nhấn Enter<br>2. Quan sát trang | **Mọi biến thể cho cùng kết quả:**<br>`a` Đăng nhập bằng Admin (`admin@example.com`)<br>`b` Đăng nhập bằng Project Manager (`EMAIL_PM` trong `.env`) | 1. Trình duyệt tự chuyển trang, **không** hiện biểu mẫu đăng nhập<br>2. Dừng ở `https://crm.anhtester.com/admin/`, tiêu đề tab có chữ `Dashboard`, vẫn đang đăng nhập đúng tài khoản cũ | Medium | Yes | UI | @Regression |
| CRM_LOGIN_TC_028 | REQ-LOGIN-20 | Bảo vệ phiên | Medium | Đang đăng nhập mà mở trang Quên mật khẩu thì được đưa về Dashboard | Đã đăng nhập bằng tài khoản theo biến thể | 1. Dán `https://crm.anhtester.com/admin/authentication/forgot_password` vào thanh địa chỉ, nhấn Enter<br>2. Quan sát trang | **Mọi biến thể cho cùng kết quả:**<br>`a` Đăng nhập bằng Admin (`admin@example.com`)<br>`b` Đăng nhập bằng Project Manager (`EMAIL_PM` trong `.env`) | 1. Trình duyệt tự chuyển trang, **không** hiện biểu mẫu Quên mật khẩu<br>2. Dừng ở `https://crm.anhtester.com/admin/`, tiêu đề tab có chữ `Dashboard` | Medium | Yes | UI | @Regression |
| CRM_LOGIN_TC_029 | REQ-LOGIN-33, REQ-LOGIN-32 | Đăng xuất | High | Sau khi đăng xuất, URL nội bộ và nút Back đều không mở lại được khu quản trị | Đã đăng nhập Admin, đang ở trang `https://crm.anhtester.com/admin/clients` | 1. Mở menu ảnh đại diện → bấm `Logout`<br>2. Thực hiện thao tác theo biến thể<br>3. Quan sát trang | **Mọi biến thể cho cùng kết quả ở bước 3:**<br>`a` Dán `https://crm.anhtester.com/admin/clients` vào thanh địa chỉ, nhấn Enter<br>`b` Bấm nút Back (←) của trình duyệt | 1. Về trang đăng nhập `https://crm.anhtester.com/admin/authentication`, không có dải thông báo nào (REQ-32)<br>3. Dừng ở `https://crm.anhtester.com/admin/authentication`; **không** xem lại được danh sách khách hàng hay nội dung nào đã mở trước khi đăng xuất | Critical | Yes | UI | @Smoke @Security @CriticalPath |
| CRM_LOGIN_TC_030 | REQ-LOGIN-39 | Đăng xuất | Medium | Đã đăng xuất chủ động thì cookie ghi nhớ còn lại cũng không tự đăng nhập lại | Chrome, cửa sổ ẩn danh mới | 1. Mở trang đăng nhập, nhập Email `admin@example.com`, Password là giá trị `PASSWORD_ADMIN` trong `.env`, **tích** `Remember me`, bấm `Login`<br>2. Mở menu ảnh đại diện → bấm `Logout`<br>3. Dán `https://crm.anhtester.com/admin/` vào thanh địa chỉ, nhấn Enter<br>4. Quan sát trang | — | 1. Vào Dashboard<br>2. Về trang đăng nhập<br>3. Trình duyệt tự chuyển trang<br>4. Dừng ở `https://crm.anhtester.com/admin/authentication`, **không** tự vào lại Dashboard<br><br>🔧 Ghi chú kỹ thuật (cần DevTools → Application → Cookies): sau bước 1 có cookie `autologin`; sau bước 2 cookie đó **vẫn còn** (`REQ-LOGIN-34` — hiện trạng, ngoài phạm vi TC) — chính vì còn cookie nên bước 4 mới chứng minh được nó không tự đăng nhập | High | Yes | UI | @Regression @Security @TechCheck |
| CRM_LOGIN_TC_031 | REQ-LOGIN-30 | Đăng xuất | Medium | Đăng xuất khi còn bộ đếm giờ công việc đang chạy thì phải xác nhận trước | Đã đăng nhập Admin. **Dựng tiền đề** (chờ PO cho phép bật timer trên môi trường dùng chung — `AMB-LOGIN-14`):<br>a. Menu trái `Tasks` → `New Task`<br>b. Điền tên task `Auto_LOGIN_TC031_<timestamp>` (timestamp lấy lúc chạy, VD `Auto_LOGIN_TC031_1790000000`) → lưu task<br>c. Ở danh sách `Tasks`, trên dòng task vừa tạo bấm `Start Timer`<br>⚠️ Form tạo task và cách dừng timer chưa khảo sát chi tiết — xem `ASM-10` | 1. Bấm biểu tượng đồng hồ trên thanh đầu trang<br>2. Mở menu ảnh đại diện → bấm `Logout`<br>3. Đọc nội dung hộp thoại<br>4. Bấm nút `Logout` trong hộp thoại<br><br>**Hậu điều kiện — luôn làm, dù PASS hay FAIL, kể cả khi dừng giữa chừng:** đăng nhập lại Admin → dừng bộ đếm giờ của task vừa tạo → `Tasks` → trên dòng task đó bấm `Delete` | Task: `Auto_LOGIN_TC031_<timestamp>` — tên sinh lúc chạy, ghi tên thật vào execution report | 1. Danh sách có đúng task `Auto_LOGIN_TC031_<timestamp>` vừa tạo ở tiền đề, đang chạy<br>2. **Không** đăng xuất ngay — bật hộp thoại xác nhận phủ màn hình<br>3. Nội dung nguyên văn `Started tasks timers found!` và `Are you sure you want to logout without stopping the timers?`, có nút `Logout`<br>4. Về trang đăng nhập `https://crm.anhtester.com/admin/authentication`<br><br>Hậu điều kiện: task không còn trong danh sách `Tasks` — môi trường dùng chung sạch dữ liệu test | Medium | Partial | UI | @Regression @E2E |
| CRM_LOGIN_TC_032 | REQ-LOGIN-42 | Bảo vệ phiên | Medium | Để yên không thao tác quá 1 giờ thì phiên hết hạn | Đã đăng nhập Admin, đang ở Dashboard. Ghi lại giờ đăng nhập | 1. Không thao tác gì với trang/tab này trong **65 phút**<br>2. Dán `https://crm.anhtester.com/admin/clients` vào thanh địa chỉ, nhấn Enter<br>3. Quan sát trang | Thời gian chờ: 65 phút (vượt mốc 1 giờ 5 phút để trừ sai số) | 2. Trình duyệt tự chuyển trang<br>3. Dừng ở `https://crm.anhtester.com/admin/authentication` như khi chưa đăng nhập — **không** vào được Customers<br><br>⏱️ TC chạy **trên 1 giờ** — không xếp vào bộ smoke | Medium | Partial | UI | @Regression @PersonalOnly |
| CRM_LOGIN_TC_033 | REQ-LOGIN-42 | Bảo vệ phiên | Medium | Đang thao tác liên tục thì phiên được gia hạn, không bị đăng xuất dù đã quá 1 giờ | Đã đăng nhập Admin, đang ở Dashboard. Ghi lại giờ đăng nhập | 1. Cứ mỗi **40 phút** bấm một mục ở menu trái (VD `Customers`, rồi `Projects`), lặp 2 lần — tổng 80 phút kể từ lúc đăng nhập<br>2. Dán `https://crm.anhtester.com/admin/clients` vào thanh địa chỉ, nhấn Enter<br>3. Quan sát trang | Chu kỳ thao tác: 40 phút · tổng 80 phút | 1. Mỗi lần bấm đều mở đúng trang, không bị đưa về trang đăng nhập<br>3. Vào được danh sách Customers `https://crm.anhtester.com/admin/clients` — phiên **vẫn còn** dù tổng thời gian đã quá 1 giờ<br><br>⏱️ TC chạy **trên 1 giờ** — không xếp vào bộ smoke | Medium | Partial | UI | @Regression @PersonalOnly |

### Quên mật khẩu

| TC ID | REQ ID | Module | Risk Level | Test Scenario | Pre-Condition | Test Steps | Test Data | Expected Result | Priority | Automation | Auto Type | Tags |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| CRM_LOGIN_TC_034 | REQ-LOGIN-25 | Quên mật khẩu | Medium | 🐞 Bỏ trống email ở Quên mật khẩu thì phải báo trường bắt buộc | Chrome, cửa sổ ẩn danh — chưa đăng nhập | 1. Mở `https://crm.anhtester.com/admin/authentication/forgot_password`<br>2. Để trống ô `Email Address`<br>3. Bấm `Confirm`<br>4. Quan sát phía trên ô Email | Email: *(trống)* | 3. Trang nạp lại, vẫn ở trang Quên mật khẩu<br>4. Dải báo lỗi báo **thiếu trường bắt buộc** cho ô Email, nhất quán với trang đăng nhập (`The Email Address field is required.`); **không** phải `Email not found`<br><br>🐞 **TC thiết kế để FAIL** trên bản hiện tại — hệ thống đang trả `Email not found` (PO xác nhận là lỗi, `AMB-LOGIN-05`), bug đang mở `BUG_login_1787226516_TC028`. **Không** sửa TC | Medium | Yes | UI | @Regression @KnownBug @Validation |
| CRM_LOGIN_TC_035 | REQ-LOGIN-26 | Quên mật khẩu | Medium | Email không tồn tại trong hệ thống thì báo không tìm thấy | Chrome, cửa sổ ẩn danh — chưa đăng nhập | 1. Mở `https://crm.anhtester.com/admin/authentication/forgot_password`<br>2. Nhập Email<br>3. Bấm `Confirm`<br>4. Quan sát trang | Email: `notexist_tc035_20260921@auto.test` | 3. Trang nạp lại<br>4. Đúng **1** dải báo lỗi `Email not found`; vẫn ở `https://crm.anhtester.com/admin/authentication/forgot_password`; không có thông báo đã gửi email | Medium | Yes | UI | @Regression |
| CRM_LOGIN_TC_036 | REQ-LOGIN-23 | Quên mật khẩu | Medium | Email sai định dạng ở trang Quên mật khẩu bị trình duyệt chặn | Chrome, cửa sổ ẩn danh — chưa đăng nhập | 1. Mở `https://crm.anhtester.com/admin/authentication/forgot_password`<br>2. Nhập Email theo biến thể<br>3. Bấm `Confirm`<br>4. Quan sát ô Email và cả trang | **Mọi biến thể cho cùng kết quả:**<br>`a` Thiếu `@` → `abc`<br>`b` Thiếu tên miền → `abc@`<br>`c` Nhiều `@` → `a@b@example.com` | 3. Trình duyệt hiện bong bóng cảnh báo ngay dưới ô Email<br>4. Trang **không** nạp lại, **không** có dải báo lỗi đỏ, chữ vừa nhập còn nguyên<br><br>⚠️ Không chấm câu chữ trong bong bóng — do trình duyệt tự sinh | Low | Yes | UI | @Regression @Validation |
| CRM_LOGIN_TC_037 | REQ-LOGIN-28 | Quên mật khẩu | Low | Liên kết đặt lại mật khẩu sai/hết hạn trả trang lỗi của trình duyệt (hiện trạng được chấp nhận) | Chrome, cửa sổ ẩn danh — chưa đăng nhập | 1. Dán URL theo biến thể vào thanh địa chỉ, nhấn Enter<br>2. Quan sát trang | **Mọi biến thể cho cùng kết quả:**<br>`a` Mã khoá sai → `https://crm.anhtester.com/admin/authentication/reset_password/1/abcdef0123456789abcdef0123456789`<br>`b` Mã người dùng không tồn tại → `https://crm.anhtester.com/admin/authentication/reset_password/999999/abcdef0123456789abcdef0123456789` | 1. Trang tải xong, không treo<br>2. Hiện **trang lỗi của chính trình duyệt** (không có logo, không có giao diện của ứng dụng), không có biểu mẫu đặt mật khẩu mới. ⚠️ chưa có evidence ảnh — xem `ASM-06`<br><br>✅ Đây là hiện trạng **được chấp nhận** (`AMB-LOGIN-03` ⏭️) — **không** kỳ vọng trang thông báo thân thiện, **không** mở bug<br><br>🔧 Ghi chú kỹ thuật (cần DevTools): phản hồi mã 500, thân rỗng | Low | Yes | UI | @Regression @NeedsVerify @TechCheck |

### Đoán lỗi & độ bền kết nối

| TC ID | REQ ID | Module | Risk Level | Test Scenario | Pre-Condition | Test Steps | Test Data | Expected Result | Priority | Automation | Auto Type | Tags |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| CRM_LOGIN_TC_038 | REQ-LOGIN-06 | Đăng nhập | High | Bấm Login hai lần liên tiếp thật nhanh vẫn vào Dashboard bình thường | Chrome, cửa sổ ẩn danh — chưa đăng nhập | 1. Mở `https://crm.anhtester.com/admin/authentication`<br>2. Nhập Email `admin@example.com`, Password là giá trị `PASSWORD_ADMIN` trong `.env`<br>3. Nhấp đúp thật nhanh vào nút `Login`<br>4. Quan sát trang | Thao tác: nhấp đúp (2 lần liên tiếp) | 3. Trang chuyển đi một lần<br>4. Dừng ở Dashboard `https://crm.anhtester.com/admin/`; **không** gặp trang `419 Page Expired!`, không có dải báo lỗi, không bị đưa ngược về trang đăng nhập | High | Yes | UI | @Regression @ErrorGuessing |
| CRM_LOGIN_TC_039 | REQ-LOGIN-21 | Đăng nhập | High | Nạp lại trang đăng nhập nhiều lần rồi mới gửi vẫn đăng nhập được (mã chống giả mạo ổn định trong phiên) | Chrome, cửa sổ ẩn danh — chưa đăng nhập | 1. Mở `https://crm.anhtester.com/admin/authentication`<br>2. Nhấn `F5` 3 lần<br>3. Nhập Email `admin@example.com`, Password là giá trị `PASSWORD_ADMIN` trong `.env`<br>4. Bấm `Login` | Số lần nạp lại: 3 | 2. Mỗi lần nạp lại trang vẫn hiện bình thường, ô nhập trống<br>4. Vào Dashboard `https://crm.anhtester.com/admin/` — **không** gặp trang `419 Page Expired!`<br><br>🔧 Ghi chú kỹ thuật (cần DevTools): biểu mẫu có trường ẩn `csrf_token_name` giá trị 32 ký tự hex; giá trị **không đổi** qua 3 lần nạp lại (gắn với cookie `csrf_cookie_name` của phiên); yêu cầu gửi biểu mẫu mang đủ `csrf_token_name`, `email`, `password` | High | Yes | UI | @Regression @ErrorGuessing @TechCheck |
| CRM_LOGIN_TC_040 | REQ-LOGIN-06 | Đăng nhập | Medium | Mất mạng đúng lúc bấm Login thì không tạo phiên; có mạng lại thì đăng nhập được | Chrome, cửa sổ ẩn danh — chưa đăng nhập | 1. Mở `https://crm.anhtester.com/admin/authentication`<br>2. Nhập Email `admin@example.com`, Password là giá trị `PASSWORD_ADMIN` trong `.env`<br>3. Ngắt mạng máy (tắt Wi-Fi / rút cáp)<br>4. Bấm `Login`<br>5. Bật lại mạng, mở lại `https://crm.anhtester.com/admin/clients`<br>6. Đăng nhập lại với cùng thông tin | Thao tác mạng: tắt rồi bật lại | 4. Trình duyệt hiện trang báo mất kết nối — không treo vô hạn<br>5. Bị đưa về trang đăng nhập — **không** có phiên nào được tạo từ lần gửi lúc mất mạng<br>6. Vào Dashboard bình thường | Medium | Yes | UI | @Regression @Reliability |
| CRM_LOGIN_TC_041 | REQ-LOGIN-06 | Đăng nhập | Medium | Mạng chậm (3G) vẫn đăng nhập hoàn tất | Chrome, cửa sổ ẩn danh — chưa đăng nhập. DevTools → Network → chọn giới hạn `Slow 3G` | 1. Mở `https://crm.anhtester.com/admin/authentication`<br>2. Nhập Email `admin@example.com`, Password là giá trị `PASSWORD_ADMIN` trong `.env`<br>3. Bấm `Login` **một lần** rồi chờ<br>4. Quan sát trang | Giới hạn mạng: `Slow 3G` | 3. Trình duyệt hiện trạng thái đang tải, không báo lỗi quá thời gian<br>4. Cuối cùng dừng ở Dashboard `https://crm.anhtester.com/admin/`, không có dải báo lỗi | Low | Yes | UI | @Regression @Reliability |

## V3 — Technical

| TC ID | REQ ID | Module | Risk Level | Test Scenario | Pre-Condition | Test Steps | Test Data | Expected Result | Priority | Automation | Auto Type | Tags |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| CRM_LOGIN_TC_042 | REQ-LOGIN-06 | Phân quyền | High | Project Manager đăng nhập vào Dashboard với menu rút gọn đúng vai trò | Chrome, cửa sổ ẩn danh — chưa đăng nhập | 1. Mở `https://crm.anhtester.com/admin/authentication`<br>2. Nhập Email, Password của Project Manager<br>3. Bấm `Login`<br>4. Đếm các mục ở menu trái<br>5. Tìm 5 mục chỉ Admin có | Email: `EMAIL_PM` trong `.env`<br>Password: `PASSWORD_PM` trong `.env` | 3. Vào `https://crm.anhtester.com/admin/`, tiêu đề tab có chữ `Dashboard`<br>4. Menu trái có đúng **9** mục (Admin có 14)<br>5. **Không** thấy `Subscriptions`, `Expenses`, `Estimate Request`, `Knowledge Base`, `Reports`<br><br>🔧 Ghi chú kỹ thuật (cần DevTools): thẻ `body` có class `user-id-3` | High | Yes | UI | @Regression @Permission @TechCheck |
| CRM_LOGIN_TC_043 | REQ-LOGIN-43 | Phân quyền | High | Tài khoản Customer đúng mật khẩu vẫn bị từ chối ở khu quản trị, thông báo không lộ tài khoản | Chrome, cửa sổ ẩn danh — chưa đăng nhập | 1. Mở `https://crm.anhtester.com/admin/authentication`<br>2. Nhập Email, Password của Customer<br>3. Bấm `Login`<br>4. Quan sát trang<br>5. Mở `https://crm.anhtester.com/login` (cổng khách hàng), đăng nhập bằng cùng tài khoản | Email: `EMAIL_CUSTOMER` trong `.env`<br>Password: `PASSWORD_CUSTOMER` trong `.env` | 3. Trang nạp lại<br>4. Đúng **1** dải báo lỗi `Invalid email or password` — giống hệt khi email không tồn tại; vẫn ở `https://crm.anhtester.com/admin/authentication`; không vào Dashboard<br>5. Đăng nhập **thành công** ở cổng khách hàng, dừng ở `https://crm.anhtester.com/` — chứng minh bước 4 không phải do sai mật khẩu | High | Yes | UI | @Regression @Permission @Security |
| CRM_LOGIN_TC_044 | REQ-LOGIN-43, REQ-LOGIN-17 | Phân quyền | High | Đang có phiên cổng khách hàng vẫn không mở được URL khu quản trị | Chrome, cửa sổ ẩn danh. Mở `https://crm.anhtester.com/login` (cổng khách hàng), đăng nhập bằng `EMAIL_CUSTOMER` / `PASSWORD_CUSTOMER` trong `.env` → dừng ở `https://crm.anhtester.com/` | 1. Dán `https://crm.anhtester.com/admin/clients` vào thanh địa chỉ, nhấn Enter<br>2. Quan sát trang | URL: `https://crm.anhtester.com/admin/clients` | 1. Trình duyệt tự chuyển trang<br>2. Dừng ở `https://crm.anhtester.com/admin/authentication`; **không** thấy danh sách khách hàng hay dữ liệu nội bộ nào | High | Yes | UI | @Regression @Permission @Security |
| CRM_LOGIN_TC_045 | REQ-LOGIN-14 | Bảo mật | High | Chuỗi tấn công trong ô Password không vượt qua được bước xác thực | Chrome, cửa sổ ẩn danh — chưa đăng nhập | 1. Mở `https://crm.anhtester.com/admin/authentication`<br>2. Nhập Email `admin@example.com`<br>3. Nhập Password theo biến thể<br>4. Bấm `Login`<br>5. Quan sát trang | **Mọi biến thể cho cùng kết quả:**<br>`a` Chèn mã SQL → `' OR '1'='1' --`<br>`b` Chèn mã SQL → `admin' #`<br>`c` Chèn script → `<script>alert(1)</script>` | 4. **Không** có hộp thoại nào bật lên<br>5. Đúng **1** dải báo lỗi `Invalid email or password`; không vào Dashboard; không có dòng báo lỗi cơ sở dữ liệu hay trang lỗi | High | Yes | UI | @Regression @Security |
| CRM_LOGIN_TC_046 | REQ-LOGIN-13 | Bảo mật | High | Chuỗi tấn công lọt qua kiểm định dạng ô Email vẫn không đăng nhập được | Chrome, cửa sổ ẩn danh — chưa đăng nhập | 1. Mở `https://crm.anhtester.com/admin/authentication`<br>2. Nhập Email theo biến thể<br>3. Nhập Password `SaiMatKhau_TC046`<br>4. Bấm `Login`<br>5. Quan sát trang | **Mọi biến thể cho cùng kết quả** (đều đúng định dạng email nên trình duyệt không chặn):<br>`a` `admin'--@example.com`<br>`b` `admin'or'1'='1@example.com`<br>`c` `x'union'select@example.com` | 4. Trình duyệt không chặn, trang nạp lại<br>5. **Bắt buộc (chấm PASS/FAIL):** không vào Dashboard; không có dòng báo lỗi cơ sở dữ liệu, trang lỗi hay trang trắng; vẫn ở `https://crm.anhtester.com/admin/authentication`<br>**Ghi nhận (không chấm):** chép nguyên văn số dải và câu chữ báo lỗi vào execution report — kỳ vọng tạm là 1 dải `Invalid email or password`, chưa đo thật với email chứa dấu `'` (`ASM-09`) | High | Yes | UI | @Regression @Security @NeedsVerify |
| CRM_LOGIN_TC_047 | REQ-LOGIN-22 | Bảo mật | High | Biểu mẫu có mã chống giả mạo bị sửa hoặc xoá thì bị chặn, không xử lý đăng nhập | Chrome, cửa sổ ẩn danh — chưa đăng nhập | 1. Mở `https://crm.anhtester.com/admin/authentication`<br>2. Sửa mã chống giả mạo của biểu mẫu theo biến thể (cách làm ở dòng 🔧)<br>3. Nhập Email `admin@example.com`, Password là giá trị `PASSWORD_ADMIN` trong `.env`<br>4. Bấm `Login`<br>5. Quan sát trang | **Mọi biến thể cho cùng kết quả:**<br>`a` Đổi thành `00000000000000000000000000000000`<br>`b` Xoá trống | 4. Trang chuyển sang trang lỗi<br>5. Tiêu đề tab `Error`; nội dung nguyên văn `419 Page Expired!` và `Sorry, the page has expired, return to previous page and refresh to continue.`; **không** vào Dashboard dù thông tin đăng nhập đúng<br><br>🔧 Ghi chú kỹ thuật (cần DevTools): ở bước 2 mở DevTools → Elements, tìm trường ẩn `csrf_token_name` trong biểu mẫu, sửa thuộc tính `value`. Máy chủ trả mã 403 dù nội dung ghi 419 — chấm theo **câu chữ**, không chấm mã (`AMB-LOGIN-08`) | High | Yes | UI | @Regression @Security @TechCheck |
| CRM_LOGIN_TC_048 | REQ-LOGIN-37 | Đăng nhập | Low | Tắt JavaScript trình duyệt, trang đăng nhập vẫn hiển thị đủ và kiểm tra dữ liệu vẫn chạy | Chrome, cửa sổ ẩn danh. Tắt JavaScript: `chrome://settings/content/javascript` → *Don't allow sites to use JavaScript* | 1. Mở `https://crm.anhtester.com/admin/authentication`<br>2. Để trống 2 ô, bấm `Login`<br>3. Nhập Email `admin@example.com`, Password là giá trị `PASSWORD_ADMIN` trong `.env`, bấm `Login`<br>4. Bật lại JavaScript | Cài đặt: JavaScript **tắt** | 1. Trang hiện đủ logo, tiêu đề `Login`, 2 ô nhập, ô `Remember me`, nút `Login`, liên kết `Forgot Password?`<br>2. Đúng 2 dải báo lỗi bắt buộc như `CRM_LOGIN_TC_009` — kiểm tra chạy ở máy chủ<br>3. Thanh địa chỉ chuyển sang `https://crm.anhtester.com/admin/` (Dashboard có thể hiển thị thiếu vì trang trong khu quản trị cần JavaScript — **không** chấm phần đó). ⚠️ chưa có evidence — xem `ASM-07`<br><br>🔧 Ghi chú kỹ thuật (cần DevTools): trang đăng nhập có 0 thẻ script nội tuyến và 0 tệp script ngoài | Low | Yes | UI | @Regression @NeedsVerify @TechCheck |
| CRM_LOGIN_TC_049 | REQ-LOGIN-44 | Bảo mật | High | 🐞 Mở trang đăng nhập bằng `http://` thì máy chủ phải chuyển sang `https://` và yêu cầu chỉ dùng HTTPS | Chrome, cửa sổ ẩn danh. **Tắt tự nâng HTTPS của Chrome** (`chrome://settings/security` → tắt *Always use secure connections*), nếu không bước 2 sẽ PASS giả | 1. Dán `http://crm.anhtester.com/admin/authentication` (http, **không** có s) vào thanh địa chỉ, nhấn Enter<br>2. Nhìn thanh địa chỉ và biểu tượng bên trái địa chỉ | URL: `http://crm.anhtester.com/admin/authentication` | 1. Trang đăng nhập hiện ra<br>2. Địa chỉ đã tự đổi thành `https://crm.anhtester.com/admin/authentication`, có biểu tượng kết nối an toàn — **không** có cảnh báo *Not secure*<br><br>🐞 **TC thiết kế để FAIL** trên bản hiện tại — bug `BUG_login_1785678750_TC039` (Critical) đang mở. **Không** sửa TC<br><br>🔧 Ghi chú kỹ thuật (cần DevTools hoặc `curl -I`): yêu cầu `http://` phải nhận chuyển hướng vĩnh viễn (301/308) kèm `Location: https://…`; phản hồi HTTPS mang header `Strict-Transport-Security`. Chấm theo **phản hồi máy chủ**, không theo thanh địa chỉ | Critical | Yes | API | @Regression @Security @KnownBug @TechCheck |

## V4 — Non-functional

| TC ID | REQ ID | Module | Risk Level | Test Scenario | Pre-Condition | Test Steps | Test Data | Expected Result | Priority | Automation | Auto Type | Tags |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| CRM_LOGIN_TC_050 | REQ-LOGIN-06, REQ-LOGIN-14 | Đăng nhập | Medium | Luồng đăng nhập đúng/sai chạy như nhau trên các trình duyệt cam kết | Trình duyệt theo biến thể, cửa sổ ẩn danh — chưa đăng nhập | 1. Mở `https://crm.anhtester.com/admin/authentication`<br>2. Đối chiếu giao diện với `CRM_LOGIN_TC_002` mục `1`<br>3. Đăng nhập Admin (`admin@example.com` + `PASSWORD_ADMIN` trong `.env`)<br>4. Mở menu ảnh đại diện → `Logout`<br>5. Đăng nhập với Password sai `SaiMatKhau_TC050` | **Mọi biến thể cho cùng kết quả** (danh sách do người dùng chốt 19-09-2026):<br>`a` Google Chrome bản mới nhất<br>`b` Microsoft Edge bản mới nhất<br>`c` Mozilla Firefox bản mới nhất | 2. Đủ 7 thành phần đúng thứ tự, không cuộn ngang, bố cục không vỡ<br>3. Vào Dashboard `https://crm.anhtester.com/admin/`<br>4. Về trang đăng nhập<br>5. Đúng 1 dải báo lỗi `Invalid email or password` | Medium | Yes | UI | @Regression @Compatibility |
| CRM_LOGIN_TC_051 | REQ-LOGIN-02 | Đăng nhập | Medium | Trang đăng nhập giữ đúng bố cục ở các kích thước màn hình đã chốt | Chrome, cửa sổ ẩn danh. Đổi kích thước bằng DevTools → Toggle device toolbar → nhập kích thước theo biến thể | 1. Mở `https://crm.anhtester.com/admin/authentication` ở kích thước theo biến thể<br>2. Thử cuộn ngang trang<br>3. Đối chiếu các thành phần | **Mọi biến thể cho cùng kết quả:**<br>`a` `1920×1080`<br>`b` `1600×750`<br>`c` `1366×700`<br>`d` `768×1024`<br>`e` `375×700` | 1. Trang hiện ra, khung đăng nhập nằm giữa theo chiều ngang<br>2. **Không** cuộn ngang được<br>3. Đủ 7 thành phần như `CRM_LOGIN_TC_002` mục `1`, không đè chữ, không bị cắt; nút `Login` rộng bằng ô nhập | Medium | Partial | UI | @Regression @Responsive |
| CRM_LOGIN_TC_052 | REQ-LOGIN-29 | Đăng xuất | Medium | Màn hình điện thoại: đăng xuất qua menu điều hướng thu gọn | Chrome, DevTools → Toggle device toolbar → `375×812`. Đã đăng nhập Admin | 1. Nhìn thanh đầu trang<br>2. Bấm nút mở menu điều hướng thu gọn trên thanh đầu trang<br>3. Tìm mục `Logout` trong menu vừa mở<br>4. Bấm `Logout` | Kích thước: `375×812` | 1. Menu trái bị ẩn, thanh đầu trang rút gọn<br>2. Menu điều hướng mở ra<br>3. Có đúng **1** mục `Logout` nhìn thấy được<br>4. Về trang đăng nhập `https://crm.anhtester.com/admin/authentication`<br><br>⚠️ chưa có evidence — hành vi đã chạy PASS ở `run_1787215085` (mã TC cũ `CRM_LOGIN_TC_033`) nhưng **không** có ảnh, và **không** tài liệu nào ghi vị trí/hình dạng nút mở menu. Lần chạy tới: chụp `logout_mobile_menu_open_375x812.png`, ghi vị trí nút vào bước 2 (`ASM-08`) | Medium | Yes | UI | @Regression @Responsive @NeedsVerify |
| CRM_LOGIN_TC_053 | REQ-LOGIN-03, REQ-LOGIN-02 | Đăng nhập | Medium | Đăng nhập hoàn toàn bằng bàn phím: thứ tự Tab hợp lý, nhãn gắn đúng ô, có viền tiêu điểm | Chrome, cửa sổ ẩn danh — chưa đăng nhập | 1. Mở `https://crm.anhtester.com/admin/authentication`, không chạm chuột<br>2. Gõ Email `admin@example.com`<br>3. Nhấn `Tab`<br>4. Gõ Password là giá trị `PASSWORD_ADMIN` trong `.env`<br>5. Nhấn `Tab`<br>6. Nhấn `Space`<br>7. Nhấn `Tab`<br>8. Nhấn `Enter`<br>9. Đăng xuất, mở lại trang đăng nhập; bấm chuột vào chữ `Password` | Phím: `Tab`, `Space`, `Enter` | 2. Chữ hiện trong ô Email — con trỏ có sẵn ở ô Email<br>3. Ô Password được chọn, có viền tiêu điểm nhìn thấy rõ<br>4. Ô Password hiện dấu chấm<br>5. Ô `Remember me` có viền tiêu điểm<br>6. Ô `Remember me` được tích<br>7. Nút `Login` có viền tiêu điểm<br>8. Biểu mẫu gửi đi, vào Dashboard `https://crm.anhtester.com/admin/`<br>9. Con trỏ nhảy vào ô Password — nhãn gắn đúng với ô | Medium | Partial | UI | @Regression @Accessibility |

## Bổ sung sau review 23-09-2026 (`054`–`060`)

> Nguồn: [testcase_review_report_web_20260923.md](../../review/testcase_review_report_web_20260923.md) — mục *Coverage Gaps* #1, #3–#8. Các gap #2 (khoảng trắng quanh mật khẩu) và #9 (cookie phiên đổi sau đăng nhập) **chưa** viết TC vì chưa có REQ neo — chốt REQ trước bằng `/update-requirements-from-ticket`.

### V2 — Required · Validation · UI Behavior · State Transition · Decision Table

| TC ID | REQ ID | Module | Risk Level | Test Scenario | Pre-Condition | Test Steps | Test Data | Expected Result | Priority | Automation | Auto Type | Tags |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| CRM_LOGIN_TC_054 | REQ-LOGIN-12 | Đăng nhập | High | Password chỉ gồm khoảng trắng thì bị coi như bỏ trống | Chrome, cửa sổ ẩn danh — chưa đăng nhập | 1. Mở `https://crm.anhtester.com/admin/authentication`<br>2. Nhập Email `admin@example.com`<br>3. Bấm vào ô Password, nhấn phím cách 3 lần<br>4. Bấm `Login`<br>5. Quan sát phía trên ô Email | Email: `admin@example.com`<br>Password: `␣␣␣` (3 khoảng trắng) | 3. Ô Password hiện 3 dấu chấm<br>4. Trang nạp lại, vẫn ở trang đăng nhập<br>5. Đúng **1** dải báo lỗi `The Password field is required.` — đối xứng với ô Email ở `CRM_LOGIN_TC_010-b`; không vào Dashboard<br><br>⚠️ chưa có evidence — kỳ vọng suy từ REQ-12 và TC_010-b, **chưa đo thật** (`ASM-11`). Nếu hệ thống trả `Invalid email or password` thì ghi nguyên văn và báo lại để chốt với PO — **không** tự sửa kỳ vọng | High | Yes | UI | @Regression @Validation @NeedsVerify |
| CRM_LOGIN_TC_057 | REQ-LOGIN-33 | Đăng xuất | Medium | Đăng xuất ở một tab thì tab khác đang mở khu quản trị cũng mất phiên | Đã đăng nhập Admin. Chưa có bộ đếm giờ công việc nào đang chạy (kiểm như tiền đề `CRM_LOGIN_TC_008`) | 1. Ở tab A, mở `https://crm.anhtester.com/admin/clients`<br>2. Mở tab B **cùng cửa sổ**, mở `https://crm.anhtester.com/admin/`<br>3. Ở tab B: mở menu ảnh đại diện → bấm `Logout`<br>4. Quay lại tab A, **không** nạp lại trang, bấm mục `Projects` ở menu trái<br>5. Quan sát tab A | — | 1. Tab A hiện danh sách Customers<br>3. Tab B về trang đăng nhập<br>4. Trình duyệt tự chuyển trang<br>5. Tab A dừng ở `https://crm.anhtester.com/admin/authentication`; **không** mở được danh sách Projects | Medium | Yes | UI | @Regression @Security @StateTransition |
| CRM_LOGIN_TC_058 | REQ-LOGIN-06 | Đăng nhập | Medium | Nhấn Enter ngay trong ô Password thì biểu mẫu được gửi đi như bấm Login | Chrome, cửa sổ ẩn danh — chưa đăng nhập | 1. Mở `https://crm.anhtester.com/admin/authentication`<br>2. Nhập Email `admin@example.com`<br>3. Bấm vào ô Password, nhập giá trị `PASSWORD_ADMIN` trong `.env`<br>4. Con trỏ vẫn ở ô Password, nhấn `Enter` — **không** bấm nút `Login`<br>5. Quan sát trang | Phím: `Enter` | 4. Trang chuyển đi, không có dải báo lỗi<br>5. Dừng ở Dashboard `https://crm.anhtester.com/admin/`, tiêu đề tab có chữ `Dashboard` | Medium | Yes | UI | @Regression @UIBehavior |
| CRM_LOGIN_TC_059 | REQ-LOGIN-26 | Quên mật khẩu | Low | Trang Quên mật khẩu: email không tồn tại có khoảng trắng thừa hoặc dài đúng biên 64 ký tự trước `@` vẫn chỉ báo không tìm thấy | Chrome, cửa sổ ẩn danh — chưa đăng nhập | 1. Mở `https://crm.anhtester.com/admin/authentication/forgot_password`<br>2. Nhập (hoặc dán) Email theo biến thể<br>3. Bấm `Confirm`<br>4. Quan sát trang | **Mọi biến thể cho cùng kết quả** (đều là email **không tồn tại** — không gửi mail thật; `␣` = 1 khoảng trắng):<br>`a` `␣␣notexist_tc059_20260923@auto.test␣␣`<br>`b` `␣␣␣notexist_tc059_20260923@auto.test`<br>`c` 64 chữ `a` liền nhau + `@auto.test` | 3. Trình duyệt **không** chặn, trang nạp lại<br>4. Đúng **1** dải báo lỗi `Email not found`; vẫn ở `https://crm.anhtester.com/admin/authentication/forgot_password`; không có thông báo đã gửi email, không có trang lỗi<br><br>⚠️ chưa có evidence — chưa đo thật (`ASM-12`). Ca **> 64 ký tự** trước `@` **không** đưa vào TC này vì chưa biết trang trả loại phản hồi nào — recon trước rồi mới viết TC riêng | Low | Yes | UI | @Regression @Validation @Boundary @NeedsVerify |
| CRM_LOGIN_TC_060 | REQ-LOGIN-12, REQ-LOGIN-13 | Đăng nhập | Low | Email sai định dạng phía máy chủ và Password trống cùng lúc thì báo đủ cả hai lỗi | Chrome, cửa sổ ẩn danh — chưa đăng nhập | 1. Mở `https://crm.anhtester.com/admin/authentication`<br>2. Dán Email theo Test Data vào ô Email<br>3. Để trống ô Password<br>4. Bấm `Login`<br>5. Quan sát phía trên ô Email | Email: 65 chữ `a` liền nhau + `@example.com`<br>Password: *(trống)* | 4. Trình duyệt **không** chặn, trang nạp lại<br>5. Đúng **2** dải báo lỗi, thứ tự trên xuống: `The Password field is required.` rồi `The Email Address field must contain a valid email address.`; không vào Dashboard<br><br>⚠️ chưa có evidence — thứ tự suy theo `CRM_LOGIN_TC_009` (Password báo trước), **chưa đo thật** (`ASM-13`). Rule `R10` ở bảng quyết định của index | Low | Yes | UI | @Regression @DecisionTable @NeedsVerify |

### V3 — Permission · Security

| TC ID | REQ ID | Module | Risk Level | Test Scenario | Pre-Condition | Test Steps | Test Data | Expected Result | Priority | Automation | Auto Type | Tags |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| CRM_LOGIN_TC_055 | REQ-LOGIN-26 | Bảo mật | High | Chuỗi tấn công đúng định dạng email ở trang Quên mật khẩu không làm lộ lỗi hay gửi mail | Chrome, cửa sổ ẩn danh — chưa đăng nhập | 1. Mở `https://crm.anhtester.com/admin/authentication/forgot_password`<br>2. Nhập Email theo biến thể<br>3. Bấm `Confirm`<br>4. Quan sát trang | **Mọi biến thể cho cùng kết quả** (đều đúng định dạng email nên trình duyệt không chặn):<br>`a` `admin'--@example.com`<br>`b` `a'or'1'='1@example.com`<br>`c` `x'union'select@example.com` | 3. Trình duyệt không chặn, trang nạp lại<br>4. **Bắt buộc (chấm PASS/FAIL):** **không** có thông báo đã gửi email đặt lại; không có dòng báo lỗi cơ sở dữ liệu, trang lỗi hay trang trắng; vẫn ở `https://crm.anhtester.com/admin/authentication/forgot_password`<br>**Ghi nhận (không chấm):** chép nguyên văn dải báo lỗi — kỳ vọng tạm `Email not found`, chưa đo với email chứa dấu `'` (`ASM-12`)<br><br>🚨 Thấy thông báo **đã gửi email** → dừng ngay, báo bug **Critical** — môi trường dùng chung, mail có thể đã gửi tới tài khoản thật | High | Yes | UI | @Regression @Security @NeedsVerify |
| CRM_LOGIN_TC_056 | REQ-LOGIN-31, REQ-LOGIN-32, REQ-LOGIN-33 | Phân quyền | Medium | Project Manager đăng xuất thì về trang đăng nhập và không mở lại được khu quản trị | Chrome, cửa sổ ẩn danh. Đăng nhập bằng `EMAIL_PM` / `PASSWORD_PM` trong `.env` → dừng ở Dashboard. **Kiểm tiền đề:** biểu tượng đồng hồ trên thanh đầu trang ghi `No started timers found`; có timer → ghi **BLOCKED** | 1. Bấm ảnh đại diện góc trên bên phải<br>2. Bấm `Logout`<br>3. Dán `https://crm.anhtester.com/admin/clients` vào thanh địa chỉ, nhấn Enter<br>4. Quan sát trang | Tài khoản: `EMAIL_PM` / `PASSWORD_PM` trong `.env` | 1. Menu ảnh đại diện mở ra, mục cuối là `Logout`<br>2. **Không** có hộp thoại xác nhận — về `https://crm.anhtester.com/admin/authentication`, không có dải thông báo nào<br>4. Dừng ở `https://crm.anhtester.com/admin/authentication`; **không** thấy danh sách khách hàng | Medium | Yes | UI | @Regression @Permission |
