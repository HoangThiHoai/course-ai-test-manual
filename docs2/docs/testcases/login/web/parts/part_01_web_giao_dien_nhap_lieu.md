# Test Cases — `LOGIN` · Web · Part 01 — Giao diện, luồng sống còn & kiểm tra dữ liệu nhập

> ⬆️ Index: [../../TEST_CASES_LOGIN_SUMMARY.md](../../TEST_CASES_LOGIN_SUMMARY.md) · 📄 Requirements: [../../../../requirements/login/web/requirements_login_web.md](../../../../requirements/login/web/requirements_login_web.md) · ➡️ Part sau: [part_02_web_phien_bao_mat_phi_chuc_nang.md](part_02_web_phien_bao_mat_phi_chuc_nang.md)

| Mục | Giá trị |
|---|---|
| **TC trong part** | `CRM_LOGIN_TC_001` → `CRM_LOGIN_TC_022` — **22 TC · 51 biến thể/mục kiểm** |
| **Vòng** | V1 Smoke (`001`–`008`) · V2a Required & Validation (`009`–`022`) |
| **Độ hạt** | GỘP — biến thể cùng trường nằm ở **Bảng biến thể** (cột `Test Data`), kiểm tra tĩnh nằm ở **Bảng kiểm** (cột `Expected Result`). Báo FAIL phải ghi mã biến thể, VD `CRM_LOGIN_TC_012-d` |
| **URL gốc** | `https://crm.anhtester.com/admin/authentication` |
| **Tài khoản** | 🔒 `admin@example.com` + mật khẩu ở biến `PASSWORD_ADMIN` trong `.env` — **KHÔNG** ghi mật khẩu thật vào tài liệu |
| **Trình duyệt chuẩn** | Google Chrome, viewport `1600×750`. TC ghi *"cửa sổ ẩn danh"* là để trình duyệt **không tự điền** Email/Password đã lưu — tự điền làm sai kết quả quan sát |

## V1 — Smoke

| TC ID | REQ ID | Module | Risk Level | Test Scenario | Pre-Condition | Test Steps | Test Data | Expected Result | Priority | Automation | Auto Type | Tags |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| CRM_LOGIN_TC_001 | REQ-LOGIN-01 | Đăng nhập | High | Mở trang đăng nhập khi chưa có phiên | Chrome, cửa sổ ẩn danh — chưa đăng nhập | 1. Mở `https://crm.anhtester.com/admin/authentication`<br>2. Nhìn tiêu đề trên tab trình duyệt<br>3. Nhìn tiêu đề giữa trang | URL: `https://crm.anhtester.com/admin/authentication` | 1. Trang đăng nhập hiện ra bình thường, không phải trang lỗi; thanh địa chỉ giữ nguyên `https://crm.anhtester.com/admin/authentication`<br>2. Tiêu đề tab ghi đúng `Perfex CRM \| Anh Tester Demo - Login`<br>3. Ngay dưới logo ANHTESTER có tiêu đề `Login`<br><br>🔧 Ghi chú kỹ thuật (cần DevTools): phản hồi của trang mang mã 200; thẻ `body` có class `login_admin` | Critical | Yes | UI | @Smoke @CriticalPath @TechCheck |
| CRM_LOGIN_TC_002 | REQ-LOGIN-02, REQ-LOGIN-03, REQ-LOGIN-36 | Đăng nhập | High | Giao diện trang đăng nhập: đủ thành phần, đúng thứ tự, đúng trạng thái mặc định | Chrome, cửa sổ ẩn danh — chưa đăng nhập | 1. Mở `https://crm.anhtester.com/admin/authentication`, **không** bấm chuột vào đâu<br>2. Đối chiếu từng mục trong Bảng kiểm<br>3. Gõ ngay `Abc12345` (vẫn không bấm chuột)<br>4. Bấm vào ô `Password`, gõ `Abc12345` | Chuỗi gõ thử: `Abc12345` | 1. Trang hiện ra<br>2. **Bảng kiểm — mọi mục phải đạt:**<br>`1` (REQ-02) Thứ tự từ trên xuống: logo ANHTESTER → tiêu đề `Login` → nhãn `Email Address` + ô nhập → nhãn `Password` + ô nhập → ô tích `Remember me` → nút `Login` → liên kết `Forgot Password?`<br>`2` (REQ-02) Ô Email và ô Password đều trống, không có chữ gợi ý mờ bên trong<br>`3` (REQ-02) Ô `Remember me` **chưa tích**, không bị mờ xám<br>`4` (REQ-02) Nút `Login` nền xanh, chữ trắng, rộng bằng ô nhập, không mờ<br>`5` (REQ-03) Ô Email có viền xanh — đang được chọn sẵn<br>`6` (REQ-36) Cả trang chỉ có **một** nút gửi là `Login`; **không** có nút đăng nhập bằng Google, Facebook, Microsoft hay dịch vụ ngoài nào<br>3. Chữ `Abc12345` hiện trong ô **Email** — con trỏ đã nằm sẵn ở đó (REQ-03)<br>4. Ô Password hiện 8 dấu chấm, không hiện chữ (REQ-02) | High | Yes | UI | @Smoke @UI |
| CRM_LOGIN_TC_003 | REQ-LOGIN-05 | Đăng nhập | Medium | Trang đăng nhập không có CAPTCHA, kể cả sau nhiều lần đăng nhập sai | Chrome, cửa sổ ẩn danh — chưa đăng nhập | 1. Mở `https://crm.anhtester.com/admin/authentication`<br>2. Quan sát toàn trang, cuộn xuống hết<br>3. Nhập Email, Password theo Test Data rồi bấm `Login`<br>4. Lặp lại bước 3 thêm 4 lần (tổng 5 lần sai)<br>5. Quan sát toàn trang | Email: `notexist_tc003_20260921@auto.test`<br>Password: `SaiMatKhau_TC003` | 2. Không có ô "Tôi không phải người máy", không có ảnh đố, không có khung xác minh nào<br>3. Trang nạp lại, 1 dải báo lỗi `Invalid email or password`<br>4. Cả 4 lần đều cùng dải báo lỗi đó<br>5. Sau 5 lần sai **vẫn không** xuất hiện CAPTCHA hay bước xác minh thêm<br><br>🔧 Ghi chú kỹ thuật (cần DevTools): trang không chứa phần tử reCAPTCHA / khung nhúng `recaptcha` nào | Medium | Yes | UI | @Regression @Security @TechCheck |
| CRM_LOGIN_TC_004 | REQ-LOGIN-04 | Đăng nhập | Low | Bấm logo trên trang đăng nhập thì về trang chủ công khai | Chrome, cửa sổ ẩn danh — chưa đăng nhập | 1. Mở `https://crm.anhtester.com/admin/authentication`<br>2. Rê chuột lên logo ANHTESTER, nhìn góc dưới trái cửa sổ trình duyệt<br>3. Bấm logo<br>4. Nhìn thanh địa chỉ và nội dung trang | URL đích kỳ vọng: `https://crm.anhtester.com/` | 2. Con trỏ chuột thành hình bàn tay; góc dưới trình duyệt hiện địa chỉ đích `https://crm.anhtester.com/`<br>3. Trình duyệt rời trang đăng nhập<br>4. Thanh địa chỉ dừng ở `https://crm.anhtester.com/` — trang chủ công khai, **không** phải một trang đăng nhập nào<br><br>🐞 Lỗi đã biết `BUG_login_1787226513_TC004`: địa chỉ gốc đang tự chuyển tiếp sang cổng đăng nhập khách hàng → bước 4 FAIL trên bản hiện tại, bước 2 vẫn PASS | Low | Yes | UI | @Regression @KnownBug |
| CRM_LOGIN_TC_005 | REQ-LOGIN-23, REQ-LOGIN-24 | Quên mật khẩu | Medium | Mở trang Quên mật khẩu từ liên kết và bằng URL trực tiếp — đủ thành phần, không có lối quay lại | Chrome, cửa sổ ẩn danh — chưa đăng nhập | 1. Mở `https://crm.anhtester.com/admin/authentication`<br>2. Bấm liên kết `Forgot Password?`<br>3. Đối chiếu từng mục trong Bảng kiểm<br>4. Mở tab mới, dán thẳng URL Quên mật khẩu, nhấn Enter | URL: `https://crm.anhtester.com/admin/authentication/forgot_password` | 2. Chuyển tới `https://crm.anhtester.com/admin/authentication/forgot_password`<br>3. **Bảng kiểm — mọi mục phải đạt:**<br>`1` (REQ-23) Dưới logo có tiêu đề `Forgot Password`<br>`2` (REQ-23) Đúng **1** ô nhập, nhãn `Email Address`, đang trống; bên dưới là nút `Confirm` nền xanh<br>`3` (REQ-23) Không có ô Password, không có ô `Remember me`<br>`4` (REQ-24) Ngoài logo, trang **không** có liên kết nào — không có `Back to login`, `Login` hay lối quay lại trang đăng nhập<br>`5` (REQ-24) Rê chuột lên logo → góc dưới trình duyệt hiện `https://crm.anhtester.com/`<br>4. Trang hiện ra giống hệt bước 3<br><br>🔧 Ghi chú kỹ thuật (cần DevTools): biểu mẫu gửi về chính URL Quên mật khẩu và có trường ẩn chống giả mạo `csrf_token_name`; trang chỉ có 1 thẻ liên kết | High | Yes | UI | @Smoke @UI @TechCheck |
| CRM_LOGIN_TC_006 | REQ-LOGIN-06 | Đăng nhập | High | Đăng nhập Admin bằng thông tin hợp lệ thì vào Dashboard | Chrome, cửa sổ ẩn danh — chưa đăng nhập | 1. Mở `https://crm.anhtester.com/admin/authentication`<br>2. Nhập Email<br>3. Nhập Password<br>4. Bấm `Login`<br>5. Quan sát trang sau khi chuyển | Email: `admin@example.com`<br>Password: giá trị `PASSWORD_ADMIN` trong `.env` | 4. Trang chuyển đi, **không** hiện dải báo lỗi nào<br>5. Thanh địa chỉ dừng ở `https://crm.anhtester.com/admin/`; tiêu đề tab **có chữ** `Dashboard` (phía trước có thể kèm số đếm thông báo, VD `(16) Dashboard` — số này thay đổi, không chấm); menu trái có mục `Dashboard` đang sáng và đủ **14** mục (`Dashboard` → `Reports`); ảnh đại diện nằm góc trên bên phải<br><br>🔧 Ghi chú kỹ thuật (cần DevTools): gửi biểu mẫu nhận mã 303 kèm chuyển hướng về `/admin/`; thẻ `body` có đủ class `app` `admin` `dashboard` `user-id-2` | Critical | Yes | UI | @Smoke @CriticalPath @TechCheck |
| CRM_LOGIN_TC_007 | REQ-LOGIN-29 | Đăng xuất | Medium | Desktop: lối đăng xuất duy nhất nằm cuối menu ảnh đại diện | Đã đăng nhập Admin (theo `CRM_LOGIN_TC_006`), viewport `1600×750` | 1. Nhìn thanh đầu trang, từ ô tìm kiếm tới góc phải<br>2. Bấm ảnh đại diện góc trên bên phải<br>3. Đối chiếu menu vừa mở theo Bảng kiểm | Viewport: `1600×750` | 1. Thanh đầu trang **không** có nút hay chữ `Logout` nào hiện sẵn<br>2. Menu thả xuống mở ngay dưới ảnh đại diện<br>3. **Bảng kiểm — mọi mục phải đạt:**<br>`1` Đúng 5 mục theo thứ tự `My Profile` → `My Timesheets` → `Edit Profile` → `Language` → `Logout`<br>`2` Mục `Language` có mũi tên nhỏ bên phải (mở menu con)<br>`3` `Logout` là mục **cuối cùng** | High | Yes | UI | @Smoke @UI |
| CRM_LOGIN_TC_008 | REQ-LOGIN-31, REQ-LOGIN-32 | Đăng xuất | High | Không có bộ đếm giờ đang chạy thì bấm Logout là đăng xuất ngay, về trang đăng nhập | Đã đăng nhập Admin; **không** có bộ đếm giờ công việc nào đang chạy | 1. Bấm biểu tượng đồng hồ trên thanh đầu trang<br>2. Bấm ra vùng trống để đóng danh sách<br>3. Bấm ảnh đại diện góc trên bên phải<br>4. Bấm `Logout`<br>5. Quan sát trang | — | 1. Danh sách thả xuống ghi `No started timers found`<br>3. Menu ảnh đại diện mở ra<br>4. **Không** có hộp thoại xác nhận nào bật lên — trình duyệt chuyển trang ngay (REQ-31)<br>5. Dừng ở `https://crm.anhtester.com/admin/authentication`, hiện biểu mẫu đăng nhập, **không** có dải thông báo nào (REQ-32) | Critical | Yes | UI | @Smoke @CriticalPath |

## V2a — Required & Validation

### Trường bắt buộc

| TC ID | REQ ID | Module | Risk Level | Test Scenario | Pre-Condition | Test Steps | Test Data | Expected Result | Priority | Automation | Auto Type | Tags |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| CRM_LOGIN_TC_009 | REQ-LOGIN-10 | Đăng nhập | High | Bỏ trống cả Email và Password thì hiện đủ 2 thông báo bắt buộc | Chrome, cửa sổ ẩn danh — chưa đăng nhập | 1. Mở `https://crm.anhtester.com/admin/authentication`<br>2. Để trống ô Email và ô Password<br>3. Nhìn nút `Login`<br>4. Bấm `Login`<br>5. Quan sát phía trên ô Email | Email: *(trống)*<br>Password: *(trống)* | 3. Nút `Login` vẫn sáng, bấm được — **không** tự khoá khi biểu mẫu trống<br>4. Trang nạp lại, vẫn ở trang đăng nhập<br>5. Đúng **2** dải báo lỗi đỏ, thứ tự trên xuống: `The Password field is required.` rồi `The Email Address field is required.`; không vào Dashboard | High | Yes | UI | @Regression @Validation |
| CRM_LOGIN_TC_010 | REQ-LOGIN-11 | Đăng nhập | High | Bỏ trống riêng Email thì chỉ báo thiếu Email | Chrome, cửa sổ ẩn danh — chưa đăng nhập | 1. Mở `https://crm.anhtester.com/admin/authentication`<br>2. Nhập Email theo biến thể<br>3. Nhập Password `SaiMatKhau_TC010`<br>4. Bấm `Login`<br>5. Quan sát phía trên ô Email | **Mọi biến thể cho cùng kết quả:**<br>`a` Email để trống hoàn toàn<br>`b` Email chỉ gồm 3 khoảng trắng `␣␣␣` | 4. Trang nạp lại, vẫn ở trang đăng nhập<br>5. Đúng **1** dải báo lỗi `The Email Address field is required.`; **không** có dải `Invalid email or password` — hệ thống không kiểm tiếp thông tin đăng nhập | High | Yes | UI | @Regression @Validation |
| CRM_LOGIN_TC_011 | REQ-LOGIN-12 | Đăng nhập | High | Bỏ trống riêng Password thì chỉ báo thiếu Password | Chrome, cửa sổ ẩn danh — chưa đăng nhập | 1. Mở `https://crm.anhtester.com/admin/authentication`<br>2. Nhập Email<br>3. Để trống Password<br>4. Bấm `Login`<br>5. Quan sát phía trên ô Email | Email: `admin@example.com`<br>Password: *(trống)* | 4. Trang nạp lại, vẫn ở trang đăng nhập<br>5. Đúng **1** dải báo lỗi `The Password field is required.`; **không** có dải `Invalid email or password` | High | Yes | UI | @Regression @Validation |

### Trường Email

| TC ID | REQ ID | Module | Risk Level | Test Scenario | Pre-Condition | Test Steps | Test Data | Expected Result | Priority | Automation | Auto Type | Tags |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| CRM_LOGIN_TC_012 | REQ-LOGIN-13 | Đăng nhập | Medium | Email sai định dạng bị trình duyệt chặn, biểu mẫu không gửi đi | Chrome, cửa sổ ẩn danh — chưa đăng nhập | 1. Mở `https://crm.anhtester.com/admin/authentication`<br>2. Nhập Email theo biến thể<br>3. Nhập Password `SaiMatKhau_TC012`<br>4. Bấm `Login`<br>5. Quan sát ô Email và cả trang | **Mọi biến thể cho cùng kết quả:**<br>`a` Thiếu `@` → `abc`<br>`b` Thiếu tên miền → `abc@`<br>`c` Thiếu phần trước `@` → `@example.com`<br>`d` Nhiều `@` → `a@b@example.com`<br>`e` Tên miền chứa `_` → `abc@exa_mple.com`<br>`f` Chữ có dấu trước `@` → `thử@auto.test` | 4. Trình duyệt hiện bong bóng cảnh báo ngay dưới ô Email<br>5. Trang **không** nạp lại, **không** có dải báo lỗi đỏ nào; dữ liệu vừa nhập ở hai ô còn nguyên; ô Email đang được chọn<br><br>⚠️ Câu chữ trong bong bóng do **trình duyệt** tự sinh, khác nhau giữa Chrome/Edge/Firefox và theo ngôn ngữ máy — **không** chấm theo câu chữ, chỉ chấm việc biểu mẫu bị chặn<br><br>🔧 Ghi chú kỹ thuật (cần DevTools): tab Network không phát sinh yêu cầu gửi biểu mẫu nào | Medium | Yes | UI | @Regression @Validation @TechCheck |
| CRM_LOGIN_TC_013 | REQ-LOGIN-14, REQ-LOGIN-15 | Đăng nhập | High | Sai email hoặc sai mật khẩu đều nhận cùng một thông báo chung, không lộ email nào có thật | Chrome, cửa sổ ẩn danh — chưa đăng nhập | 1. Mở `https://crm.anhtester.com/admin/authentication`<br>2. Nhập Email theo biến thể<br>3. Nhập Password theo biến thể<br>4. Bấm `Login`<br>5. Quan sát trang<br>6. So sánh màn hình kết quả của biến thể `a` và `b` | **Mọi biến thể cho cùng kết quả ở bước 5:**<br>`a` Email không tồn tại `notexist_tc013_20260921@auto.test` · Password `SaiMatKhau_TC013`<br>`b` Email có thật `admin@example.com` · Password sai `SaiMatKhau_TC013`<br>`c` Email đúng định dạng có ký tự đặc biệt `qa.login+tc013_20260921@auto.test` · Password `SaiMatKhau_TC013` | 4. Trang nạp lại<br>5. Đúng **1** dải báo lỗi `Invalid email or password`; vẫn ở `https://crm.anhtester.com/admin/authentication`; không vào Dashboard<br>6. `a` và `b` **giống hệt nhau** về câu chữ, số dải báo lỗi và địa chỉ trang — nhìn kết quả không đoán được email nào có tài khoản (REQ-15) | Critical | Yes | UI | @Smoke @Security @Validation |
| CRM_LOGIN_TC_014 | REQ-LOGIN-13, REQ-LOGIN-14 | Đăng nhập | Medium | Biên hợp lệ: phần trước `@` dài 63 và đúng 64 ký tự vẫn được coi là đúng định dạng | Chrome, cửa sổ ẩn danh — chưa đăng nhập | 1. Mở `https://crm.anhtester.com/admin/authentication`<br>2. Dán chuỗi Email theo biến thể vào ô Email<br>3. Bấm vào ô Email, nhấn phím `End`<br>4. Nhập Password `SaiMatKhau_TC014`<br>5. Bấm `Login`<br>6. Quan sát trang | **Mọi biến thể cho cùng kết quả:**<br>`a` 63 chữ `a` liền nhau + `@example.com` (tổng 75 ký tự)<br>`b` 64 chữ `a` liền nhau + `@example.com` (tổng 76 ký tự) | 3. Cuối ô vẫn hiện đủ `@example.com` — ô **không** tự cắt bớt ký tự<br>5. Trình duyệt **không** chặn; trang nạp lại<br>6. Đúng **1** dải báo lỗi `Invalid email or password` — hệ thống đã coi email hợp lệ về định dạng và kiểm tới bước thông tin đăng nhập | Medium | Yes | UI | @Regression @Boundary |
| CRM_LOGIN_TC_015 | REQ-LOGIN-13 | Đăng nhập | Medium | Biên vượt: phần trước `@` dài hơn 64 ký tự bị máy chủ từ chối ở bước kiểm định dạng | Chrome, cửa sổ ẩn danh — chưa đăng nhập | 1. Mở `https://crm.anhtester.com/admin/authentication`<br>2. Dán chuỗi Email theo biến thể vào ô Email<br>3. Bấm vào ô Email, nhấn phím `End`<br>4. Nhập Password `SaiMatKhau_TC015`<br>5. Bấm `Login`<br>6. Quan sát trang | **Mọi biến thể cho cùng kết quả:**<br>`a` 65 chữ `a` + `@example.com`<br>`b` 100 chữ `a` + `@example.com`<br>`c` 250 chữ `a` + `@auto.test` (ca của `BUG_login_1787226515_TC018` — đã đóng, *không phải lỗi*) | 3. Cuối ô vẫn hiện đủ phần tên miền — ô không tự cắt ký tự<br>5. Trang nạp lại<br>6. Đúng **1** dải báo lỗi `The Email Address field must contain a valid email address.` — **khác** dải của `CRM_LOGIN_TC_014`; không vào Dashboard<br><br>ℹ️ Mốc 64 ký tự trước `@` là giới hạn của chuẩn email RFC 5321 — xem `ASM-02` ở index | Medium | Yes | UI | @Regression @Boundary |
| CRM_LOGIN_TC_016 | REQ-LOGIN-07 | Đăng nhập | Medium | Email không phân biệt chữ hoa/thường khi đăng nhập | Chrome, cửa sổ ẩn danh — chưa đăng nhập | 1. Mở `https://crm.anhtester.com/admin/authentication`<br>2. Nhập Email theo biến thể<br>3. Nhập Password là giá trị `PASSWORD_ADMIN` trong `.env`<br>4. Bấm `Login`<br>5. Quan sát trang<br>6. Mở menu ảnh đại diện → bấm `Logout` để chạy biến thể tiếp theo | **Mọi biến thể cho cùng kết quả:**<br>`a` `ADMIN@EXAMPLE.COM`<br>`b` `ADMIN@Example.COM`<br>`c` `Admin@example.com` | 4. Trang chuyển đi, không có dải báo lỗi<br>5. Dừng ở `https://crm.anhtester.com/admin/`, tiêu đề tab có chữ `Dashboard`<br>6. Về trang đăng nhập | High | Yes | UI | @Regression @Validation |
| CRM_LOGIN_TC_017 | REQ-LOGIN-38 | Đăng nhập | Medium | Email có khoảng trắng thừa ở đầu/cuối vẫn đăng nhập được | Chrome, cửa sổ ẩn danh — chưa đăng nhập | 1. Mở `https://crm.anhtester.com/admin/authentication`<br>2. Nhập Email theo biến thể (giữ nguyên khoảng trắng)<br>3. Nhập Password là giá trị `PASSWORD_ADMIN` trong `.env`<br>4. Bấm `Login`<br>5. Quan sát trang<br>6. Mở menu ảnh đại diện → bấm `Logout` để chạy biến thể tiếp theo | **Mọi biến thể cho cùng kết quả** (`␣` = 1 khoảng trắng):<br>`a` `␣␣admin@example.com␣␣` — 2 khoảng trắng mỗi đầu<br>`b` `␣␣␣admin@example.com` — chỉ ở đầu<br>`c` `admin@example.com␣␣␣` — chỉ ở cuối | 4. Trang chuyển đi, không có dải báo lỗi, trình duyệt **không** chặn<br>5. Dừng ở `https://crm.anhtester.com/admin/`, tiêu đề tab có chữ `Dashboard`<br>6. Về trang đăng nhập | High | Yes | UI | @Regression @Validation |

### Trường Password

> Đối soát bảng Field-Level loại **Password** (9 mục) — xem mục *Đối soát Field-Level* ở index. Các mục độ dài tối thiểu/tối đa, ký tự đặc biệt, chữ hoa/thường bắt buộc, chữ số bắt buộc **không áp dụng** ở màn hình đăng nhập (`AMB-LOGIN-09` ✅: không có chính sách mật khẩu nào).

| TC ID | REQ ID | Module | Risk Level | Test Scenario | Pre-Condition | Test Steps | Test Data | Expected Result | Priority | Automation | Auto Type | Tags |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| CRM_LOGIN_TC_018 | REQ-LOGIN-14 | Đăng nhập | High | Mật khẩu phân biệt chữ hoa/thường — đúng ký tự nhưng sai kiểu chữ thì bị từ chối | Tài khoản test `qa_login_case@auto.test` có mật khẩu `Auto@Login2026` do QA lead cấp (xem `ASM-05` — mật khẩu tài khoản demo toàn chữ số nên không đảo kiểu chữ được). Chrome, cửa sổ ẩn danh | 1. Mở `https://crm.anhtester.com/admin/authentication`<br>2. Nhập Email `qa_login_case@auto.test`<br>3. Nhập Password theo biến thể<br>4. Bấm `Login`<br>5. Quan sát trang | **Mọi biến thể cho cùng kết quả:**<br>`a` Đảo toàn bộ kiểu chữ → `aUTO@lOGIN2026`<br>`b` Chỉ đổi chữ cái đầu → `auto@Login2026` | 4. Trang nạp lại<br>5. Đúng **1** dải báo lỗi `Invalid email or password`; không vào Dashboard | High | Partial | UI | @Regression @Validation |
| CRM_LOGIN_TC_019 | REQ-LOGIN-02 | Đăng nhập | Medium | Ô Password che ký tự, cho dán, không có nút hiện/ẩn mật khẩu | Chrome, cửa sổ ẩn danh — chưa đăng nhập. Mở sẵn Notepad chứa chuỗi `Abc12345` | 1. Mở `https://crm.anhtester.com/admin/authentication`<br>2. Bấm vào ô Password, gõ `Abc12345`<br>3. Nhìn quanh ô Password tìm nút hiện/ẩn<br>4. Xoá ô Password; sao chép `Abc12345` từ Notepad, bấm vào ô Password, nhấn `Ctrl+V`<br>5. Nhập Email `admin@example.com`; xoá ô Password, dán mật khẩu thật (giá trị `PASSWORD_ADMIN` trong `.env`) bằng `Ctrl+V`<br>6. Bấm `Login` | Chuỗi gõ/dán thử: `Abc12345` | 2. Ô hiện đúng 8 dấu chấm, không hiện chữ<br>3. **Không** có biểu tượng con mắt, chữ `Show`/`Hiện` hay nút nào để xem mật khẩu<br>4. Dán được, ô hiện 8 dấu chấm — trang **không** chặn dán<br>5. Ô hiện dấu chấm che toàn bộ<br>6. Vào Dashboard `https://crm.anhtester.com/admin/` — mật khẩu dán vào được chấp nhận như gõ tay | Medium | Yes | UI | @Regression @Validation @UIBehavior |
| CRM_LOGIN_TC_020 | REQ-LOGIN-14 | Đăng nhập | Medium | Mật khẩu rất dài hoặc chứa chữ có dấu, emoji không làm hệ thống lỗi | Chrome, cửa sổ ẩn danh — chưa đăng nhập | 1. Mở `https://crm.anhtester.com/admin/authentication`<br>2. Nhập Email `admin@example.com`<br>3. Dán Password theo biến thể<br>4. Bấm `Login`<br>5. Quan sát trang | **Mọi biến thể cho cùng kết quả:**<br>`a` 256 chữ `p` liền nhau<br>`b` 1000 chữ `p` liền nhau<br>`c` Có dấu + emoji → `Mật_Khẩu😀2026` | 4. Trang nạp lại trong vài giây, không treo<br>5. Vẫn ở trang đăng nhập, bố cục bình thường, đúng **1** dải báo lỗi `Invalid email or password`; **không** có trang lỗi, trang trắng hay dòng báo lỗi kỹ thuật | Medium | Yes | UI | @Regression @Boundary @Localization |

### Ô tích Remember me

| TC ID | REQ ID | Module | Risk Level | Test Scenario | Pre-Condition | Test Steps | Test Data | Expected Result | Priority | Automation | Auto Type | Tags |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| CRM_LOGIN_TC_021 | REQ-LOGIN-08 | Đăng nhập | Medium | Tích Remember me rồi đăng nhập thành công, hệ thống cấp cookie ghi nhớ | Chrome, cửa sổ ẩn danh mới (chưa có cookie nào của `crm.anhtester.com`) | 1. Mở `https://crm.anhtester.com/admin/authentication`<br>2. Nhập Email<br>3. Nhập Password<br>4. Bấm vào **chữ** `Remember me` (không bấm ô vuông)<br>5. Bấm `Login`<br>6. Quan sát trang | Email: `admin@example.com`<br>Password: giá trị `PASSWORD_ADMIN` trong `.env` | 4. Ô `Remember me` chuyển sang có dấu tích — bấm vào chữ cũng tích được<br>5. Trang chuyển đi, không có dải báo lỗi<br>6. Vào Dashboard `https://crm.anhtester.com/admin/`<br><br>🔧 Ghi chú kỹ thuật (cần DevTools → Application → Cookies): xuất hiện cookie `autologin` có hình thái `a:2:{s:7:"user_id";s:1:"<id>";s:3:"key";s:16:"<16 ký tự hex>";}` — chỉ kiểm **hình thái và độ dài key = 16**. 🔒 **KHÔNG** chép giá trị thật vào báo cáo | Medium | Yes | UI | @Regression @TechCheck |
| CRM_LOGIN_TC_022 | REQ-LOGIN-09 | Đăng nhập | Medium | Không tích Remember me thì đăng nhập vẫn thành công và không có cookie ghi nhớ | Chrome, cửa sổ ẩn danh mới (chưa có cookie nào của `crm.anhtester.com`) | 1. Mở `https://crm.anhtester.com/admin/authentication`<br>2. Nhìn ô `Remember me`<br>3. Nhập Email, Password, **không** tích `Remember me`<br>4. Bấm `Login`<br>5. Quan sát trang | Email: `admin@example.com`<br>Password: giá trị `PASSWORD_ADMIN` trong `.env` | 2. Ô `Remember me` đang **chưa tích**<br>4. Trang chuyển đi, không có dải báo lỗi<br>5. Vào Dashboard `https://crm.anhtester.com/admin/`<br><br>🔧 Ghi chú kỹ thuật (cần DevTools → Application → Cookies): **không** tồn tại cookie `autologin`. Phải chạy trên cửa sổ ẩn danh mới — cookie còn sót từ lần đăng nhập trước làm sai kết quả | Medium | Yes | UI | @Regression @TechCheck |
